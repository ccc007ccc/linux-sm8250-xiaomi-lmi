[package]
name = "lmi-camera"
version = "0.1.0"
edition = "2024"
description = "Rust control-plane runtime for the lmi OV13B10 local camera path"
license = "GPL-2.0-only"

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
